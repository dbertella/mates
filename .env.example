# Copy to .env and fill in your values. Used for email on new activity.

# Recipient for new-activity emails
EMAIL_TO=you@example.com

# Optional: From address (defaults to SMTP user)
# EMAIL_FROM=

# SMTP (e.g. Gmail: use an App Password, not your normal password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your@gmail.com
SMTP_PASS=your-app-password
